# firepanda specification

Written August 2026, against Mojo 1.0 (Modular 26.5), pandas 3.0.5, Polars 1.43 and
Arrow 25.0.0. Start with [00-README.md](00-README.md), which is the real index and
says what was already decided and why.

| | |
|---|---|
| [00-README.md](00-README.md) | Index, settled decisions, prerequisites, honesty about scope |
| [01-research-2026.md](01-research-2026.md) | State of Mojo, pandas, Polars, Arrow and the Mojo ecosystem, with sources |
| [02-architecture.md](02-architecture.md) | Layers, memory, types, plan, optimizer, execution |
| [03-dtype-dispatch.md](03-dtype-dispatch.md) | Compile-time monomorphization and the generated dispatch table |
| [04-python-dx.md](04-python-dx.md) | The two front doors, and every deliberate divergence from pandas |
| [05-kernels.md](05-kernels.md) | Kernel shape, the hash table, strings, the GPU path |
| [06-pandas-parity.md](06-pandas-parity.md) | The full pandas 3.0 conformance checklist, milestone-tagged |
| [07-python-bindings.md](07-python-bindings.md) | `PythonModuleBuilder`, Arrow PyCapsule, wheels, and the ABI problem |
| [08-milestones.md](08-milestones.md) | M0 through M11 including M4b for SQL, exit criteria, and four points to stop and reassess |
| [09-quality-bar.md](09-quality-bar.md) | Testing, and living without a race detector |
| [10-benchmarks.md](10-benchmarks.md) | What gets measured and against whom |
| [11-package-layout.md](11-package-layout.md) | The tree, and why Mojo 1.0's import rules decide it |
| [12-the-python-front-door-measured.md](12-the-python-front-door-measured.md) | Document 07 checked against a running toolchain, with what works, what does not, and what it cost |
| [13-the-bound-type-is-not-a-dataframe.md](13-the-bound-type-is-not-a-dataframe.md) | What `PythonTypeBuilder` cannot do, and why the pandas surface has to be a Python object |
| [14-errors-across-the-boundary.md](14-errors-across-the-boundary.md) | How one Mojo `Error` becomes the seven Python exception classes document 07 promised |
| [15-the-arrow-capsule-boundary.md](15-the-arrow-capsule-boundary.md) | How a frame reaches pyarrow and Polars with no copy, and who owns the memory afterwards |
| [16-reading-somebody-elses-arrow.md](16-reading-somebody-elses-arrow.md) | The Arrow stream in both directions, what the real producers taught the checks, and how DuckDB finally reads a frame |
| [17-the-column-you-can-hold.md](17-the-column-you-can-hold.md) | The bound `Series`, what `df[key]` does with each kind of key, how a column exports itself, and where the hand written members go |
| [18-making-a-frame-out-of-python.md](18-making-a-frame-out-of-python.md) | How `pd.DataFrame({...})` becomes an Arrow frame, what the inference decides, and where the pandas constructor signature lives |
| [19-finding-a-label-in-an-index.md](19-finding-a-label-in-an-index.md) | The lookups and the set operations on `Index`, why one factorize answers all of them, and what the unmaterialized range is worth |
| [20-editing-an-index.md](20-editing-an-index.md) | The five operations that change an index's labels, the three that turn a label into a slice bound, and what an unsorted index costs |
| [21-the-index-you-can-hold.md](21-the-index-you-can-hold.md) | The third bound type, why `df.index` returns an index rather than a list, and the divergences the Python facing index has |
| [22-what-a-message-is-for.md](22-what-a-message-is-for.md) | Why an error message leads with pandas' sentence and follows with ours, which three kinds are deliberately not copied, and why the code asks a question rather than reading its own strings |
| [23-a-typo-and-a-gap.md](23-a-typo-and-a-gap.md) | Why a value pandas refuses and a value pandas takes that firepanda has not written are different classes of error, why not every fixed vocabulary gets a check, and why an argument in a signature is not always an argument the call reads |
| [24-a-column-that-arrives-as-codes.md](24-a-column-that-arrives-as-codes.md) | How a dictionary encoded Arrow column is read, why the value type has to be taken off the schema before it is released, why the codes travel by the ordinary path and the categories go around, and what happens when two batches disagree about what a code means |
| [25-writing-a-category-back-out.md](25-writing-a-category-back-out.md) | Why a dictionary field carries the format of its codes rather than of its values, why the hung schema and array are found through the member rather than through the box, why the categories are the one thing the exporter copies, and what a round trip has to keep |
| [26-building-a-category-out-of-a-column.md](26-building-a-category-out-of-a-column.md) | Why encoding a dictionary is not a cast and lives in a kernel of its own, why the categories come out sorted rather than in first appearance order, why a cast off a category has to decode before it converts, and what the index width is going to cost later |
| [27-what-a-caller-does-with-a-category.md](27-what-a-caller-does-with-a-category.md) | Why the eleven names on `Series.cat` are three operations underneath, why the arithmetic between them belongs on the Python side, why a rename does not check the count, why every read widens the codes, and the three differences from pandas that are asserted rather than fixed |
| [28-comparing-a-category.md](28-comparing-a-category.md) | Why a comparison on a category column never needs a promotion and is answered before the dispatch, the six rules pandas has for it, why a scalar that is not a category is false under equality and an error under an ordering, and why the ordering follows the categories rather than the words |
| [29-a-category-that-survives-being-moved.md](29-a-category-that-survives-being-moved.md) | Why eight kernels that move rows around all lost a column's categories while keeping its type, why the one input kernels can carry the list across and the two input ones have to refuse instead, why `where` on a category column did not work at all, and why the conformance suite found this and the unit tests did not |
| [30-a-position-in-a-string.md](30-a-position-in-a-string.md) | Why the twelve `str` methods that came first are a group, why every string kernel in the library counts bytes and is right to, why a character slice cannot size its output in parallel the way `substr` does, why a slice bound crosses the boundary as an absence, and why `index` and `rindex` are not in the extension at all |
| [31-a-window-is-a-pair-of-rows.md](31-a-window-is-a-pair-of-rows.md) | Why the five window parameters are one question, why an expanding window is a rolling one with no near end, why pandas cannot get an infinity back out of a rolling sum and this can, why the compensation is kept beside the total rather than folded into the next row, and why `count` gates on how many rows a window covers rather than on how many hold a value |
| [32-a-window-that-is-a-weight.md](32-a-window-that-is-a-weight.md) | Why an exponentially weighted window is a second kernel rather than a third window shape, why the four spellings of the decay collapse to one number before they cross the boundary, why one recurrence covers both readings of `adjust`, why a corrected weighted variance needs two weight totals, and why pandas' own answer differs from the same recurrence by one bit |
| [33-an-index-of-instants.md](33-an-index-of-instants.md) | Why `DatetimeIndex` is a Python subclass of `Index` and not a fourth bound type, why the import graph decided which struct each of the three new bindings went on, why the calendar members are the `dt` accessor reached through another door, what answers an `Index` and what answers a `DatetimeIndex`, and why sixty eight pandas members are deliberately absent |
| [34-the-two-ends-of-a-row.md](34-the-two-ends-of-a-row.md) | Why nine of the fifty seven `str` methods are one piece of work, why a width is a count of characters, where Python's twenty nine whitespace code points come from, why the odd character of a centred row goes where CPython's `unicode_center` puts it, how nine methods crossed the boundary without adding an argument to it, and which side of the boundary each of pandas' four refusals lives on |
| [35-a-column-that-is-also-the-labels.md](35-a-column-that-is-also-the-labels.md) | Why `set_index` had to come before the rest of the indexing section, why moving a column into the labels is a move rather than a conversion, why the round trip back through `reset_index` is the whole specification, why `sort_index` has a narrow fast path, why `searchsorted` does not check that the index is sorted, why `isin` has a Python fallback, and what each of the twelve refusals would cost to lift |
| [36-a-position-and-a-label-are-different-questions.md](36-a-position-and-a-label-are-different-questions.md) | Why `loc` and `iloc` are one piece of work, why the shape of the key decides the shape of the answer, why a key is read on one side of the class and applied on the other, why the columns are narrowed before the rows, why one cell is a crossing of its own rather than a column and a read, why the row as a series is refused rather than guessed at, and why a negative position is resolved in the binding |
| [37-choosing-columns-by-a-rule.md](37-choosing-columns-by-a-rule.md) | Why `filter`, `select_dtypes` and `truncate` are one piece of work, why a frame can be asked what it holds without reading what it holds, why the type tree is numpy's and is copied without tidying, why `include="object"` is refused in one method and not softened for another, why two of `filter`'s three rules keep the frame's order and one does not, and why `truncate` checks the pair before it works out the direction |
| [38-which-row-of-a-repeat-stays.md](38-which-row-of-a-repeat-stays.md) | Why `duplicated` and `drop_duplicates` are one piece of work, why the three `keep` rules are one table read twice, why the kernel reads the codes and nothing else, why both passes are serial, why a missing value repeats another missing value here and is dropped in `group_by`, why the boundary carries a word for all three rules, why a repeated column name is accepted in one layer and refused in the other, and what the mask found wrong with the index |
| [39-the-n-best-rows-of-a-column.md](39-the-n-best-rows-of-a-column.md) | Why `nlargest` and `nsmallest` are the grouped top n with one group in it, why keeping the last of a tie is the column read backwards rather than a second rule in the kernel, why the count is cut down before the slot table is built, why `keep="all"` and a second ranking column are refused rather than guessed at, and how a missing name is told apart from a column that cannot be ranked |
| [40-a-label-the-frame-does-not-have.md](40-a-label-the-frame-does-not-have.md) | Why `reindex` is two operations under one name, why the fill value is a row appended to the column rather than a pass over the answer, why a null the frame already had is left alone, why the integer to float widening is a pandas rule that lives above the gather, where the block of one value repeated went and which two copies of it were left alone, why an empty set of labels is answered before the lookup, what the eight parameters that do no work are told, and the two answers here that are deliberately not pandas' answers |
| [41-an-index-that-answers-as-a-column.md](41-an-index-that-answers-as-a-column.md) | Why forty nine names missing from the index and from the datetime index at once are one bridge rather than forty nine features, why the bridge is a free function on the module rather than a method on either type, why the labels come back twice, the name that means unnamed on each side of the boundary, what the nine members built on it cost, why `dropna` hands back the class it was given where every other member does not, why `nunique` answers a parameter the column it is built on refuses, and what numpy's arguments to `min` and `max` are told |
| [42-a-column-that-answers-as-a-frame.md](42-a-column-that-answers-as-a-frame.md) | Why ten methods the frame had and the column did not are one door rather than ten features, why that door is a free function on the module for the third time, why the labels have to come across for any of the ten to be a line, the column name that is a number in pandas and a string here, why `duplicated` is the one that does not take its own column back, and what the index gets for free out of two doors end to end |
| [43-sorting-by-value-rather-than-by-label.md](43-sorting-by-value-rather-than-by-label.md) | Why a finished sort sat in the core unreachable from Python and what that says about measuring a surface, where pandas' vocabulary for a sort and the core's differ and which side undoes each difference, why two arguments are accepted and never read while every other one is read or refused, why `ignore_index` is implemented here and refused on `sort_index`, why a null gets a real position here where pandas gives a negative one, why `Index.sort_values` has two return shapes, and why the column does not go through the frame this time |
| [44-copying-when-nothing-can-be-written.md](44-copying-when-nothing-can-be-written.md) | What `copy` is for in pandas and why that reason is gone in a library with no way of writing into a frame, why `deep` is unobservable and both values of it are satisfied by the same answer, why the frame and the series share their extension object while the index does not, what `Index.is_` has to do with that, why the class comes from `type(self)`, why the copy protocol is defined without scoring anything, and what the thirty four inplace divergence cases were asserting while they could not run |
| [45-renaming-which-moves-a-name-and-not-a-row.md](45-renaming-which-moves-a-name-and-not-a-row.md) | The two things pandas spells `rename`, why renaming a column or a level name is a change to a schema and renaming a row label is a pass over the index, which of the six callables and one property are here and which refuse, why `level` has to be None and refuses zero the way pandas does, why `errors` is the one parameter that changes an answer, why `inplace` is honoured on the index and nowhere else, why `rename_axis(axis=1)` has nowhere to write the name, and which kernel the label half is waiting on |
| [46-dropping-a-column-and-dropping-a-row.md](46-dropping-a-column-and-dropping-a-row.md) | The second word pandas gives to two operations, why dropping a column is a schema edit and dropping a row label is a gather over every column, why the row half is `Index.drop` and `reindex` composed rather than a kernel and what the extra hashing pass costs, why that composition refuses a repeated row label where pandas does not, the three ways of naming the wrong door and which class each one is, why `Series.drop(columns=...)` is accepted and does nothing, why the missing names are collected above the boundary for one half and below it for the other, and why a misspelled `errors` raises here and does not there |
| [47-filling-what-is-missing-with-a-value-that-was-named.md](47-filling-what-is-missing-with-a-value-that-was-named.md) | The smallest operation in this section under one of the most written lines in pandas, why the kernel was finished and the method did not exist, why a firepanda column is `Int64` and not `int64` and what that makes the rules for which values a column can hold, why the check is on the kind of the value rather than on whether a cast exists, why a number into a text column raises here and widens to object there, why a column with nothing missing is left alone whatever was offered and why that needs a new way to ask a frame about its gaps, why `limit` is a different kernel rather than a parameter, and which two shapes of `value` are an alignment waiting on a door that is already half open |
| [48-choosing-each-row-against-a-condition.md](48-choosing-each-row-against-a-condition.md) | Why `where` and `mask` are one method with the condition turned over and the one correction that reading needs, why a row the condition says nothing in is replaced by both of them and what that decides about the order of two lines, why the kernel grew a false side of one row and why the row that holds nothing is a pass of its own, why the answer keeps the column's type where pandas widens and why pandas' own nullable types are the argument for it, why a column that keeps every row is answered untouched before the other side is looked at, how a missing value of a column's own type is built out of a gather from nowhere, why a run of values against a frame is one per column rather than one per row, and the shape of this family that pandas cannot currently run at all |
| [49-holding-every-value-between-two-bounds.md](49-holding-every-value-between-two-bounds.md) | Why `clip` is two comparisons and two picks over the kernel document 48 built and needed no kernel of its own, why a value that is missing is not clipped without anybody writing that rule, the three different things that mean no bound and why one of them is per row, why two bounds that are both values are put in order and two that carry rows are not, why both comparisons are made against the column as it arrived and what that answers when the bounds cross, why a label a bound does not carry loses its value while a row it carries nothing in keeps it, why a bound that is a fraction is only a problem when a row actually takes it, why a run of values against a frame is a value per column here and a reshape there, and the one shape this takes that pandas leaks on |
| [50-swapping-some-values-for-others.md](50-swapping-some-values-for-others.md) | Why `replace` turned out not to need the per value mapping kernel it was predicted to need and is a comparison and a pick per pair instead, why every pair is judged against the column as it arrived and how that answers both the swap and the absence of any cascade without a rule being written, why the last of two pairs that match a row wins for free, the difference between a value nothing holds and a value nothing could hold and why exactly one refusal about types is swallowed, why a missing value named for replacement is `isna` rather than a comparison, why a mapping on a frame means column names with a value beside it and values without one and why that makes a common call quietly do nothing, the four refusals pandas spells out and the one that carries a trailing space, and where the hash table would actually start to win |
| [51-putting-the-answer-back-where-it-came-from.md](51-putting-the-answer-back-where-it-came-from.md) | Why `inplace` turned out to need no design for owned buffers and was refused for five slices against a question pandas stopped asking in 3.0, what an inplace call in pandas under copy on write can and cannot be seen by and why the answer is nothing except a second name for the same object, why a wrapper here holding one slot makes putting an answer back a rebind rather than a write, the undocumented split between the pandas methods that answer None and the ones that answer the object itself and why `Series.rename` sits on the other side from `DataFrame.rename`, the four things pandas will and will not accept as a flag including a one that is refused and a None that is not, the one refusal that stays because a column has nowhere to put a frame, and why none of this is faster in either library |
| [52-multiplying-and-asking-whether-a-column-is-true.md](52-multiplying-and-asking-whether-a-column-is-true.md) | Why a product cannot borrow the loop the sum uses and what the null holds a zero invariant costs it, why an empty product needs no seen flag where an empty minimum does, how any and all end up as one body with the return of the empty case doubling as the fallthrough, why a NaN has to be missing rather than true and what the obvious loop answers instead, what pandas actually answers for skipna=False and bool_only and an empty string, why a length of time has a truth value and a point in time does not, the axis=None fold this slice found already broken and the split between the six reductions that can be built out of their own answers and the six that cannot, and why product is not a fourth code in the kernel |
| [53-a-column-with-no-name.md](53-a-column-with-no-name.md) | Why a column with no name and a column called nothing are two states rather than one, what pandas does differently with each, why the fix is the `Optional[String]` an index level name has had since it was written, why a frame column cannot hold an absence and what that costs the seven members that go out to a frame and back, why the absence must not be put back by asking whether the name came out empty, and what a conformance board comparing whole objects is for. |
| [54-reducing-a-group-with-a-product-and-with-a-truth.md](54-reducing-a-group-with-a-product-and-with-a-truth.md) | Why the boundary already knew all three words and only the kernel was missing, what the null holds a zero invariant costs a grouped product that a grouped sum spends for free, why a product needs no seen table where a minimum does and what that table costs per group per worker, why a truth is one body with a conditional store and why it cannot stop early the way the whole column form can, why the rule for what counts as true lost its underscore and is now read from both paths, why any and all are the only two grouped reductions a text column can answer, and the pandas signatures measured rather than remembered including the engine arguments a grouped sum has and a grouped product does not |
| [55-asking-whether-a-value-is-one-of-a-set.md](55-asking-whether-a-value-is-one-of-a-set.md) | Why the kernel and the core method were both finished before the slice started and only a binding was missing, why the set crosses the boundary as a column rather than as a list, why dropping the values a column cannot hold is the same answer as comparing them and is not a shortcut, the two places where a flag and a number are the same value in pandas, how four missing values per dtype are reached from one null by reading the set instead of the column, why a nan needs a second pass that a null does not, why a category is looked up through its codes rather than through a second category, what putting a frame back together through the constructor costs and what a kernel would save, and why a frame or a series as the argument is an aligned comparison wearing a membership test's name |
| [56-walking-a-frame-and-a-column.md](56-walking-a-frame-and-a-column.md) | Why a missing Python protocol is worse than a missing method, what the `__getitem__` fallback did to `list(s)` and why it gave three different outcomes on three different sets of labels, why a frame iterates its column names and a column iterates its values, why `in` on a column asks about labels and never looks at a value even though the line does not read that way, why a key of a kind no label could be is answered rather than refused, why `keys` is the axis through a second name, why `items` is lazy on a frame and eager on a column, why `itertuples` reads each column once rather than each cell once, why the renaming of a field that could not be a name is left to the standard library, and why `iterrows` and iterating a group by are both waiting on something else |
| [57-how-big-a-frame-is-and-what-it-holds.md](57-how-big-a-frame-is-and-what-it-holds.md) | Why the members that do no work are the first ones anything else reads and why a frame that cannot answer them does not look like a frame, why `empty` is a question about axes rather than about rows and why writing it as a row count is right on every frame built by hand and wrong on the one being checked for, why `size` counts cells on a frame and rows on a column and why there is no error available to catch the confusion, why a class with one axis has a member that answers a list of one and what loop that shape is for, why the column types are a column rather than a list and what two names have to be taken off the thing that carries them, why the types are strings here and objects there and why that belongs to `dtype` rather than to this, and why `nbytes` is a gap on a column and not a gap on a frame |
| [58-how-much-memory-a-frame-is-using.md](58-how-much-memory-a-frame-is-using.md) | Why the two libraries are measuring two different things under one name and why neither number is wrong, what `nbytes` counts and why the validity bitmap is in it even when nothing is missing, why that means no frame anywhere agrees exactly and nobody should go looking for one, why a dictionary column counts its codes and its categories both, why the answer is a column on a frame and a number on a column, why a frame with a column called `Index` gets two rows with the same label and why that is left alone, why the default counts the index and `nbytes` never does and why that is the only reason both members exist, why `deep` is accepted and ignored rather than refused, and what a caller should compare each number against |
| [59-what-a-frame-says-about-itself.md](59-what-a-frame-says-about-itself.md) | Why `info` is formatting and nothing else now that every number in it is a member, how the table is laid out down to the trailing spaces and the rule that is shorter than its column, why the position column is five characters wide on a narrow frame and six on a wide one, which three lines differ from pandas' and why all three are registered divergences rather than new ones, why there is never a `+` after the memory number, why a missing label prints as `None` here and `nan` there, why the labels line reads its own class name rather than having one written into it, why the limit between the two forms is a constant with pandas' default in it, why the missing counts are always taken here and are not always taken there, and why nothing in the signature is refused |
| [60-the-labels-down-the-side.md](60-the-labels-down-the-side.md) | Why a defect only the renderer had was invisible to a suite that reads values and visible to everybody who looks at a frame, why the renderer could not reach the labels and why breaking the cycle was the wrong fix, how the labels arrive already rendered and why only the eleven on screen are ever built, why the gap in the label column cannot drift out of step with the gap in the value column, why the labels are left aligned when the values are not and why a numeric index is left aligned too, why the name of the level goes above the listing on a column and inside the table on a frame, why the frame's name row ends in trailing spaces, why an empty column says nothing about its labels, and why a missing label keeps this library's spelling rather than pandas' |
| [61-the-place-in-front-of-a-value.md](61-the-place-in-front-of-a-value.md) | Why a whitespace difference on every numeric column is worth a slice of its own and what it was costing the conformance cases, the one rule underneath all of it which is that every value is written one place in and a minus spends that place, why a text column named `aaaaaa` is one character narrower than an integer column named the same, why a boolean counts as numeric for this and where that is visible, why a timestamp keeps no place at all, why the elision is two dots in a narrow column and three in a wide one and why the width it asks about is the rendering rather than the data, why the dots are centred on a column and right aligned on a frame and left aligned under the labels, what `str.center` does with the odd space and why that is transcribed rather than reasoned about, why the elided column is four wide in every row including the one carrying the index name, and why the footer says the name before the length |
| [62-one-format-for-a-whole-column.md](62-one-format-for-a-whole-column.md) | Why every float value in a column looks right on its own and the column is still wrong, why `2.0` prints as `2.000` when it sits beside a longer number and why no rule applied to the value can produce that, why everything is written fixed at six places first and what the two passes over the whole column do afterwards, why the trailing zeros come off the column rather than off the value and why one place always survives, why the switch to an exponent needs a length over twelve and a magnitude over a million at the same time and why every simpler model of it is wrong, why a value smaller than the last place printed sends the column to an exponent on its own and why `1e-6` is the boundary, why the elision happens before any of it and why a value in the elided middle cannot decide the format, why each column decides alone and why the labels down the side are a column too, why a tie rounds to the even digit and which four values prove it, and which two things in the implementation are equivalences rather than rules |
| [63-what-a-missing-value-is-called.md](63-what-a-missing-value-is-called.md) | Why the last difference the printing group left behind is kept rather than fixed and why that needs an argument when everything else in the group was a bug, why pandas has four spellings for a missing value rather than one and what each of them is telling you about the storage underneath, why `<NA>` is pandas' own spelling for a column that keeps its gaps in a mask and why every column here is such a column, why the difference is not about printing at all and what a list read out of a half null float column shows, what it costs a reader diffing two renderings and why one extra character moves every value in the column, what it does not change and which registered entry already covers the comparison, why one spelling is the point rather than an oversight, and the two things the registry entry does not cover |
| [64-the-case-of-a-letter.md](64-the-case-of-a-letter.md) | Why the eight `str` names about case ship as one group and what the short answers are, why pandas has two answers for the case of a letter depending on how the column is held and which of the two this library copies, why the count of characters is preserved and the count of bytes is not, why the walk is borrowed from the standard library and why an element has to be checked for being well formed UTF-8 before it is handed over, what a sweep of every code point through both sides measures about where the borrowed data is not Arrow's data, why the 149 code points that rewrite wrongly are corrected from a committed table of pairs and why the 1384 that answer a question wrongly needed a table of a different shape, how the check for needing a correction stays off the fast path, which registered entry covers the one divergence the group adds to the board, how `capitalize` and `swapcase` are written out of the same corrected mappings plus a list of the thirty one titlecase characters and why neither of them asks a case question, why `casefold` follows Python rather than Arrow and is the one name here that can give back a longer row, why `title` waited for a table of a different shape before it could ship, and which names of the accessor are left once every question about what a character is has been answered |
| [65-the-classes-a-character-belongs-to.md](65-the-classes-a-character-belongs-to.md) | Why asking what a character is takes a different shape of data from asking what it maps to, what a sweep of every code point measures about the 1384 the Mojo standard library and Arrow disagree on across the three class questions, why the tables are read out of pyarrow rather than out of the published Unicode files and what that costs, how a class is held as ranges written flat and read by the parity of a lower bound search, why the first 128 code points are two words and no search at all, what the three questions actually are at the level of a row and why the two about case are not opposites in two separate ways, why the titlecase characters are their own class that both case questions have to read, why the board does not move when a library stops being wrong in the general case, why `title` and `istitle` are the first two names here whose answer is not decidable a code point at a time and what the rule about where a word starts actually turns out to be, why neither of them needed a mapping table because Arrow titlecases a character by upper casing it, why `isascii` needed nothing at all and is the one question a row of nothing answers yes to, what the four remaining classes cost and why `isalnum` needed none of them, why the simple rule the five of them share was measured against Arrow rather than assumed by analogy with the case questions, and why Arrow's idea of a digit is wider than Python's by 877 code points so that pandas calls a half sign a digit and `str.isdigit` does not |
| [66-a-pattern-that-is-only-itself.md](66-a-pattern-that-is-only-itself.md) | Why four names pandas answers with a regular expression engine can ship without one, what a pattern holding none of the twelve metacharacters means to an engine and to a byte search and why those are the same thing, why a pattern that does hold one is refused by name rather than searched for literally and why a confident wrong answer is worse than either of the two things the board knows how to read, why three of the four were already written as `LIKE` shapes and only the count needed a kernel, why matches do not overlap, why an empty pattern is counted in bytes rather than characters and what the three ways pandas can hold the same column each answer, why that is the same road the case classes took, why `case` and `flags` are refused rather than ignored, what the group does to the board and what the corpus check came back with this time, and what the sixteen names left actually need |
| [67-the-one-pattern-that-needs-no-promise.md](67-the-one-pattern-that-needs-no-promise.md) | Why the fifth name about a pattern is the one that narrows nothing, since `regex` defaults to False in pandas 3 and the ordinary call is a literal already, why it is the first kernel in the pattern file whose answer is text and therefore the first that cannot be allocated up front or run across morsels, why matches do not overlap here for the same reason they do not in the count, why an empty pattern is counted in characters here and in bytes in `count` on the same row in the same accessor and what measurement of a pyarrow kernel that does not terminate explains it, why that split is unstable in a way a divergence registry cannot hold, what the three ranges of `n` mean and which of them corrected my own expectation, which four arguments are refused and why exactly one of the four is a gap rather than a shortfall since pandas does honour `case=False` here, why a mapping pattern is unpacked before the crossing rather than after, why this is the first name in the accessor to arrive with nothing at all to register including on the null heavy frame, which two corpus changes were the board's own gap rather than the library's and why a frame that is the alphabet cut at every length cannot test a limit, and why the case folding kernel is now owed to five places |
| [68-a-table-is-not-a-small-replace.md](68-a-table-is-not-a-small-replace.md) | Why a table of single characters is a different answer from several replacements rather than a faster spelling of them, since every key is applied in the same pass and so a table that swaps two characters really swaps them where the same pair given to `replace` collapses both, why what a key maps to is never looked at again, why this is the first `str` name in a while with no argument about which backend pandas is reading out of since the table goes straight to Python and no pyarrow kernel is in the path at all, why `None` and the empty string are one request and the crossing therefore carries no case for a delete, why a key that is not an integer is dropped rather than refused and what that means for a table that does half of what it looks like it does, how the lookup keeps two seats and when a row is walked byte by byte instead of decoded, why a mapping is required where pandas takes anything subscriptable and why a refusal is the more honest of the two available answers, why this name gets a door of its own without being the drift document 07 warns about since the rule there is about answer shape and the thing that is different here is the argument, why the table is sorted before the crossing rather than after, and what shape this shares with the label half of `rename` |
| [69-the-fold-a-search-is-not-the-fold-a-reader-gets.md](69-the-fold-a-search-is-not-the-fold-a-reader-gets.md) | Why the argument four names refused for five documents turned out to need a second Unicode table rather than the one `casefold` already had, what the measurement of `STRASSE` against `straße` says about the difference between a fold that may make a row longer and a fold that may not, why lowercasing is the wrong guess in four places a search cares about and casefolding is the wrong guess in 104, how the rule was recovered from pyarrow rather than read out of a Unicode file and why the generator sweeps all 1.1 million code points before it will write anything, why `replace` alone has its answer decided by Python's `re.IGNORECASE` rather than by Arrow and what checking the two against each other bought, why the pattern is folded once and the row is never folded at all, what the skip table and the wide scan both lose the moment the comparison stops being about bytes, why a folded match can cover a different number of bytes than the pattern it matched and which two functions had to change shape because of it, why the fold is spelled as a second kernel and a longer word rather than as a flag and why that is what `strip_chars` already is, and why `n=0` means no replacements to one half of pandas and every replacement to the other half of the same method |
| [70-an-answer-that-is-wider-than-a-column.md](70-an-answer-that-is-wider-than-a-column.md) | Why the first two `str` names whose answer is a frame rather than a column were the right next slice, since they force the question of what an accessor does with three columns without also needing a regular expression engine or a list column type, what the difference between the two names really is given that searching from the right is only half of it and the other half is that a row the separator is not in goes first for one name and last for the other, why there is no argument here about which backend pandas reads out of since pyarrow has no partition kernel at all and both pandas backends loop in Python calling CPython's own method row by row, why the kernel searches once and fills three builders rather than answering one column three times, why the three come back as a list and not a tuple and what that has to do with `StringArray` not being implicitly copyable, why a door of its own is the answer shape rule working rather than the drift document 07 warns about and how that differs from what `translate` needed, why the column labels are text where pandas uses integers and why that is registered rather than worked around, what a column with no readable rows shows about pandas deciding both its refusals and the width of its answer one row at a time, and why `expand=False` is refused by name alongside the five other methods waiting on the same missing type |
| [71-an-answer-that-is-narrower-than-a-column.md](71-an-answer-that-is-narrower-than-a-column.md) | Why `str.cat` folding a column into a single string is the same door rule as `partition` answering three columns rather than the opposite decision, how one name carries two operations that share nothing but their arguments and why only the half without `others` is written, why the row by row half is refused rather than approximated given that pandas aligns the two columns on their labels first and concatenating by position would be quietly right for some callers and quietly wrong for the rest, what a missing row does and why that is two answers behind one argument since `na_rep=""` keeps the row and its separator where leaving the argument out drops both, why an empty row reaches the same output by the opposite rule and has to be tested beside it, why the kernel counts the bytes before writing any of them and what the growing twin is really checking, and why the refusals here are the first in this series that are not pandas' own sentences, since pandas checks neither `sep` nor `na_rep` nor `join` and lets the first two fall into `str.join` |
| [72-an-answer-whose-width-is-in-the-data.md](72-an-answer-whose-width-is-in-the-data.md) | Why `str.get_dummies` is the third answer shape on this accessor and the first whose width nobody can predict from the arguments, since the number of columns and their names are both properties of the data, what a token is at the edges of a row given that an empty row and a separator at either end and two separators together all contribute the same empty string label, why the answer is a membership and not a count even though the columns come back as int64, why a missing row is a row of zeros and is the one place on this accessor a missing row does not stay missing, why the columns come back in byte order and why that is code point order and must not consult a locale, why the kernel reads the column twice and holds its token set as a sorted list rather than a hash table, why any test of a byte offset has to hold a character wider than a byte or it is not testing the offset, and why the one disagreement with pandas is pandas' own frame constructor failing on a column of only missing rows rather than pandas deciding anything |
| [73-which-regular-expression-pandas-actually-answers.md](73-which-regular-expression-pandas-actually-answers.md) | Why the premise in the regular expression issue is wrong and pandas runs two engines rather than one, how it picks between them by parsing the pattern with Python's own parser and routing to Python when it finds a lookaround or a backreference and to Arrow's RE2 otherwise, which five methods can reach RE2 and which four never do so that `contains` and `findall` give different answers for `\d` on the same column, why adding a lookahead that excludes nothing changes which characters count as digits, the four differences between the two engines that are visible in answers rather than only in refusals and the several places they agree that were plausible places to differ, why pandas rewriting a trailing `\Z` to `\z` is upstream papering over one instance of that class, the patterns Python reads and RE2 refuses so that pandas raises an Arrow sentence for a pattern its own engine would have run, the corner where a lookaround standing behind syntax Python cannot parse is invisible to the router and the call is answerable by neither engine, and what all of that means for what has to be built here |
| [74-the-mean-of-a-set-of-points-in-time.md](74-the-mean-of-a-set-of-points-in-time.md) | Why the same library answered a point in time from the frame and a count of seconds from SQL for the same average over the same column, why a divergence of that shape survives every test either half has since each half is consistent with itself, why the schema function was the hole given that it declared a float64 it knew was not what either path produced, why reading the one temporal table from both sides is the only fix that cannot drift back apart, what comes with that table beyond the types since it is also the list of reductions that have no answer and those refusals now land at bind time about the query the user wrote rather than about a sum hidden inside a mean, why a mean over a column of times cannot fold at all given that its running state would have to hold a sum of points in time, why holding the column and calling the whole frame kernel makes the two front doors the same call rather than the same rule written twice, what that costs in memory and why it lands on a rare query, why the reductions that do still fold lose their label in the accumulator and where the label goes back on, and why the whole column flag is the one thing about an aggregate's type that is a property of the node rather than of the expression |
| [75-the-one-name-answered-by-the-standard-library.md](75-the-one-name-answered-by-the-standard-library.md) | Why `str.normalize` is the one name on this accessor whose answers come from CPython rather than from Arrow, since pandas defines it once as `unicodedata.normalize` applied a row at a time and there is no Arrow normalization kernel at all, so the authority for a name is whichever library pandas actually calls for it rather than a rule about the accessor, why it is also the only name here whose rule is about sequences rather than about characters so that the answer for a character depends on its neighbours, why the four forms are two independent choices and the kernel takes two flags rather than a form name, why the decomposition tables are fully expanded at generation time so that nothing recurses and no depth has to be bounded, why the composition table is derived by asking CPython to compose every candidate pair rather than by reading the exclusion list and reimplementing the three rules behind it, why Hangul costs no table and why an ASCII element is its own answer under all four forms, the six rows that separate a right implementation from a plausible one including the singleton, the excluded pair and the UAX 15 example where NFC and NFKC are different characters neither of which is reachable from the other, and why both refusals are pandas' own and are read twice |
| [76-two-engines-and-the-parser-that-picks-one.md](76-two-engines-and-the-parser-that-picks-one.md) | Why the grammar that decides which of the two engines answers a call is Python's even though the engine that answers most calls is RE2, so that the set of patterns Python's parser refuses is part of the observable behaviour of a pandas program and a parser that read `\p{L}` out of helpfulness would break it, why a parse failure is a value on the way out rather than a raised error given that the caller above is a router with a question, why the nodes live in one flat arena rather than a tree of nested lists and why that is both a Mojo constraint and the shape the compiler behind this wants, which of Python's own collapses are reproduced and why the test is whether a collapse is visible in the two answers a caller can see rather than whether it is in Python's tokens, the eight rules that had to be read out of `re/_parser.py` rather than reasoned about including what a quantifier repeats when a comment is in the way and when a backslash and some digits are an octal number instead of a backreference, the five of seven nesting op codes the router upstream does not walk into so that `(?=a)` is answered and `(?=a)?` raises an Arrow error naming a library the caller did not call, why the empty negative lookaround collapses into a node that routes to the engine which cannot run it while the positive one on the same line does not, why a generated corpus of thirty thousand patterns came before the hand written tests and what the first run found that no hand written test would have, the one pattern where copying pandas exactly was not available because its parser raises something pandas does not catch, what `\N{NAME}` is read as until there is a Unicode name table and why the parse says so rather than leaving a pattern that quietly matches the replacement character, and the pattern whose routing depends on which CPython is loaded |
| [77-the-machine-that-runs-a-pattern-once.md](77-the-machine-that-runs-a-pattern-once.md) | Why the engine that runs a compiled pattern holds every place the pattern could be at once and reads the text once rather than backtracking, since a pattern arrives from a caller and is run against a column and an engine that can take exponential time on `(a+)+b` is a denial of service with a friendly API in front of it, why a refusal carries a flag saying whether RE2 refuses the pattern too or firepanda cannot answer it yet and why that one bit moves every hold out decision out of heuristics over the pattern text and into the compiler where the facts about RE2 already are, why the refusal is a walk over the whole tree rather than something noticed while instructions are written given that a backreference under a `{0}` compiles to nothing at all, the constructs RE2 refuses that were measured rather than read including a backslash in front of any character outside ASCII and a one digit octal escape inside a class, why RE2's limit of a thousand repeats is a budget that divides on the way into a repeat rather than a check on one number so that `(a{11}){91}` is an error, the three constructs both engines read differently rather than refuse and why they are gaps here since none of them raises anywhere and a wrong answer is a column of booleans that looks exactly like a right one, why RE2 answers the word boundary question between bytes so that pandas' two string backends disagree about `\B` on the same data, what the differential's first run found in three families that no hand written test would have, and why captures were left out of a machine that was otherwise ready for them |
| [78-three-questions-that-are-one-question.md](78-three-questions-that-are-one-question.md) | Why `contains`, `match` and `fullmatch` are one kernel with the pattern rewritten rather than three kernels, since pandas itself anchors the pattern and asks the same question, why the rewrite happens after the router has decided which engine a pattern goes to because the rewrite can change that decision, why a trailing dollar sign in a pattern being anchored for `fullmatch` is the one place upstream reads its own rewrite wrongly and answers a literal dollar, what the `na` argument fills and why it is a list walk rather than a kernel, and why a pattern holding no metacharacter still goes to the byte search so that the fast path stays the common path |
| [79-counting-is-not-asking-four-times.md](79-counting-is-not-asking-four-times.md) | Why counting the matches in a row is a different scan from asking whether there is one rather than the same scan repeated, what Arrow actually does when it counts so that a match of no width is counted where it is found rather than where the cursor is, why the row is handed to the engine again from after each match with the text before it thrown away so that a boundary or a caret reads the remainder as a whole row, the three rules that came out of measuring pandas rather than out of reading either engine, why `str.count("")` counts bytes rather than characters upstream and why this library does not copy that, and the two upstream observations about a counting loop that steps in bytes while the replacing loop beside it steps in characters |
| [80-replacing-is-a-third-loop.md](80-replacing-is-a-third-loop.md) | Why writing a row out with its matches swapped is a third scan rather than a caller of either of the two before it, since the answer's length is not known until the scan has run so the rows go into a builder and the kernel is serial until there is a builder per morsel, why the replacement has a grammar of its own so that a literal shortcut would read it the other way round and answer a different question, why a count and a pattern together are refused rather than copied given that Arrow's bounded path puts five markers at the front of a row it then leaves alone and raises on a boundary against plain ASCII, why `n=0` means nothing to one pandas path and everything to another on the same column in the same call, and the seven upstream observations a bounded replace turned up |
| [81-the-same-letter-in-two-alphabets.md](81-the-same-letter-in-two-alphabets.md) | Why `\w` is 63 characters of ASCII for six methods on the `str` accessor and 138558 code points for the other three, since six of them go to Arrow and get RE2's reading and three of them never reach Arrow at all, why the second engine is the machine that was already there compiled differently with three things settled while the pattern compiles, why the three classes are generated out of the running CPython rather than out of the published Unicode files and why the generator checks all three against the matching string methods before it writes, why `\d` is exactly `str.isdecimal` and not `str.isdigit` which is 128 code points wider, why a record that a pattern holds syntax RE2 refuses is a fact about the other engine so that three families of pattern stopped being refused without any new grammar, why every refusal gained an owner so that a lookaround is agreement with upstream for one engine and a shortfall for the other, and what the differential's 26608 compared patterns and 3444 held out are made of |
| [82-an-answer-whose-width-is-in-the-pattern.md](82-an-answer-whose-width-is-in-the-pattern.md) | Why `str.extract` is the fourth answer shape on this accessor and the first whose width is neither a constant nor a property of the data but is settled the moment the pattern compiles, why it never consults the router since pandas has no Arrow path here to fall back from so the pattern is read in Python's alphabet for every call, why it searches where `match` and `fullmatch` anchor, the three rules about a missing answer that look like one until a row disagrees with itself and the fourth state that reads like missing and is a value, why the group labels are turned from two lists into one at the end of the compiler and ride on the program rather than being worked out again from the pattern, why an unnamed group's position written out as text is the divergence `partition` already carries and why a group name can never collide with one, why a column answered by a single unnamed group keeps the name it was called on rather than losing it, and why `expand` is checked before the pattern and the group count after it |
| [83-a-flag-spent-before-the-first-row.md](83-a-flag-spent-before-the-first-row.md) | Why `(?i)` is spent while the pattern compiles and never reaches a row, so that a literal becomes the set of everything that folds onto it and the machine never learns a flag was set, why the two documents before this one predicted two fold tables and the measurement found one table of 2927 code points in 1468 groups plus a four code point exception for the Turkish I, how both engines were asked and the two checks that make the answer safe which are closure against all 1114112 code points and a refusal to write the file if the difference is anything but those four, why a class is folded and then negated since the other order makes `(?i)[^a]` match almost everything, why RE2's ASCII Perl classes are not closed under folding so `(?i)\w` matches the Kelvin sign and the fold has to be spent per item inside the brackets, why a letter with no other case stays one instruction, why the first table shape cost seventy one seconds of Mojo compile time in every test file that reached the compiler and what Go's cycle and run encoding cut it to, why `\b` is folded on neither engine even though the class beside it is, and the pandas defect where `str.match(pat, flags=X)` raises for every flag but IGNORECASE |
| [84-the-same-fold-asked-for-from-outside.md](84-the-same-fold-asked-for-from-outside.md) | Why `case=False` on a regular expression, `flags=re.IGNORECASE` and a written `(?i)` are one fact by the time anything acts on them, since pandas compiles the pattern with the argument and then zeroes the argument, why the flags are seeded into the parser rather than merged afterwards so that both alphabet checks see an argued flag and both of `match`'s parses carry it through the anchoring rewrite, why the measurement split the planned slice in two when it found that `match` alone keeps a flagged pattern on Arrow while `contains`, `fullmatch` and `count` hand any flag at all to Python's engine whose counting and replacing scans are the ones document 79 measured, why the fold is asked for with a word ending in `_folded` rather than with a second argument, why `match` carries two `ValueError`s nobody else has and why the case-sensitivity one fires before the flags one, why `match`'s `case` default had to move from True to nothing, and what the Python engine's own scan is still holding up |
| [85-the-flag-that-picks-an-engine.md](85-the-flag-that-picks-an-engine.md) | Why a `flags` argument means two things at once and only one of them is about the pattern, so that the letters can ride in the seeded tree the way document 84 seeds them while the routing has to be told separately because `case=False` arrives as the same bit and stays on RE2, why the mask door grew a number rather than 128 words per method and why that number being nonzero is the route, why the seven letters are numbered differently by `re` and by the kernel and why a value holding an eighth bit is refused rather than dropped, why an argued call is anchored with `\A` and `\z` rather than with the rewrite Arrow needs since a dollar sign moves under multiline and sits before a trailing newline in any case, how the differential turned up a fifth difference between the two engines in Python's `\B` failing on an empty subject which is a CPython special case rather than a consequence of any rule about word characters, why the locale flag is a `ValueError` on both sides while verbose and ascii are gaps, why a flag beside `regex=False` is refused rather than dropped the way upstream drops it, why the type check on the pattern had to move to the top, and what the counting and replacing halves of the same scan are still holding up |
| [86-the-other-half-of-the-scan.md](86-the-other-half-of-the-scan.md) | Why the rule document 85 used to pick an engine does not survive `replace`, since a replacement holding `\g<` and an empty pattern both reach Python's engine with no flags at all, so the engine moves back into the word and the number is left holding only what the letters mean, why the engine then rides on the compiled program rather than being threaded through four signatures that would do nothing with it, what the one rule Python's two loops share is where Arrow's counting loop has three and what the three differences look like in ordinary answers, why the replacing loop needs two cursors and how a limit comes out wrong with one, the replacement template grammar measured escape by escape including the lookahead that tells `\123` from `\12`, why `case=False` on this one name is not the one to one fold it is on the four names above it since upstream turns it into the flag and compiles the pattern with it, which makes `replace_folded` unreachable from the pandas layer and costs a byte search, the three refusals that came off that were never about flags at all including the count of zero that means everything on one path and nothing on the other, and why `re.PatternError` not being a `ValueError` is a divergence this library cannot match without a worse surface |
| [87-the-flag-with-nowhere-to-go.md](87-the-flag-with-nowhere-to-go.md) | Why `str.extract` is the one pattern method whose `flags` argument says one thing rather than two, since pandas never sends it to Arrow under any argument so there is no route for the letters to carry and nothing for the word at the door to say, why the kernel had honoured every flag since the day it was written because `program_for` compiles for Python's engine before anything routes, why the letters are read before the groups are counted for the same reason upstream reads them first and because verbose mode is the one flag that changes what the characters around a bracket mean, why the type check on the pattern moved to the entrance for the second time in three slices and what that says about a validation that lives where the work happens, which of the seven letters are answered and which two are still the parser's gap, why `re.LOCALE` is upstream's own refusal rather than a copy of one, that `re.DEBUG` is a real flag pandas answers by printing the parsed pattern, which discriminating row separates each letter from its absence in a frame of groups rather than in a mask, and why the mask that refuses an unknown flag bit had been dropping every bit `re` never named since it was written, because oring seven `IntFlag` members gives an `IntFlag` whose complement is bounded by the enumeration rather than by the integer |
| [88-a-grammar-and-an-alphabet.md](88-a-grammar-and-an-alphabet.md) | Why verbose mode is a rule about where the skip happens rather than about which characters are skipped, since the class loop and the counted scan and the peek for a lazy marker are all separate reads and none of them skips, which is one line of code and three behaviours a reader would call inconsistent, why the whitespace set is the six characters `re` names rather than Unicode's or `str.isspace`'s so that a file separator in a pattern still means something, why `a{1, 2}` matches the literal text `a{1,2}` written without the space rather than matching nothing, why the flag is read off the cursor at the top of each pass rather than restarting the parse the way CPython does and what rule about global flag groups makes that the same answer, why the ascii flag could not reuse RE2's tables and left three alphabets where there were two, since Python's ASCII `\s` holds a vertical tab and RE2's never did, why the narrowed fold makes `(?ia)` a third answer rather than either of the two already there and why the negated class is the row that checks it, why the word boundary follows the alphabet onto the branch RE2 already uses, what came off the refusal list and why a scoped flag group did not, and the one character wide row that separates this letter from its absence in a pandas frame |
| [89-a-letter-with-a-reach.md](89-a-letter-with-a-reach.md) | Why a flag that applies to part of a pattern could not be a wider field on the parse, since a set of letters with no place attached is enough to refuse a pattern on and not enough to answer one, why the node is this library's own rather than CPython's shape, since Python hangs the two flag sets on a subpattern whose group number is None and a subpattern here carries a capture number that is a payload rather than an option, why six of the seven letters are handled by the compiler and verbose mode by the parser, since verbose mode decides which characters there are rather than what a character means, why nesting is a save and a restore rather than a clear and which row of the tests would pass either way, why the `narrow` reading is recomputed instead of saved beside the flags it is read from, why RE2 needed no new reasoning at all and still refuses the four letters it never had, why the router had to be taught the node because upstream's walk enters a scoped group and a pattern holding a lookahead inside one would otherwise reach an engine that has never heard of it, and why the ascii and unicode conflict is right by construction here rather than by a case, and the one cell that parts company with pandas, which is a CPython defect where the first character optimisation reads the pattern's own flags rather than the ones in force inside a scoped group, so `re.search` skips a row `re.fullmatch` matches and three of the five pattern methods in one accessor inherit a wrong answer that the other two do not |
| [90-a-rule-with-a-date-on-it.md](90-a-rule-with-a-date-on-it.md) | Why a rule copied off a running interpreter is a measurement with a date on it, and what happened when one of them moved underneath a library that had written the measurement down and not the date, what CPython 3.14 did to `\B` on an empty subject and how the two suites came to be asking two different interpreters without either of them being wrong, how many of the held-out corpus's 30052 patterns answer differently on 3.13 and on 3.14 and which two families all 280 of them fall into, why the version is a minor number on the builder rather than a boolean saying whether one rule is on, since two rules moved in the same release and a range of supported versions is made of numbers, why the empty row case is an instruction of its own rather than the third position code document 88 gave it, since `(?a)` narrows which characters count as word characters and says nothing at all about a row that holds none of any kind, what that buys back, which is `\b` and `\B` being a pair of pairs again under both alphabets with the invented code deleted, where the interpreter is read and why it is read at the door rather than passed in by a caller who did not choose it, why RE2 has no version of Python and no `\B` either, and what the sixteen texts a sweep asks cannot find |
| [91-a-spelling-and-a-version.md](91-a-spelling-and-a-version.md) | Why the second of the two rules CPython 3.14 moved is a smaller change than the first and a more awkward one to implement, since `\z` became a legal spelling of a position Python already had rather than a position becoming sayable, how pandas answers the same pattern over the same column with a column or with a `bad escape` depending on a keyword argument that says nothing about escapes, why the refusal cannot be driven off the parse tree at all since both spellings build the same node and have to, which makes this the third field on the parse that records a fact about the caller's text rather than about the tree, what was found while trying to drive it off anything, which is that this library was writing RE2's spelling of the end of the string into a pattern it builds for Python's engine and had been getting away with it because its own parser reads both, why that is wrong on its own terms rather than only in the way it blocked this fix, and why the Arrow rewrite in the same file spelling the same position the other way round is two engines rather than an inconsistency, where the refusal sits in the compiler's order and why it is a `ValueError` and not a gap, which three separate things have to be true before a set bit means the caller wrote one, and why the job that checks the newest version constant is the next thing now rather than a someday thing |
| [92-the-light-that-comes-on-by-itself.md](92-the-light-that-comes-on-by-itself.md) | Why a compiler holding a version number for a rule it copied out of somebody else's library fails quietly rather than loudly, and what has to exist before the next release is a red light on purpose rather than one by luck, why this was deferred once on the grounds that a mechanism for keeping a list of one honest is a mechanism nobody maintains and why the condition that document set for building it has now been met, which three things are checked and why the one that matters most is the one that fails when a rule has retired, since a threshold the supported floor has risen past can never fire again and nothing else in the repository would ever notice that the constant and its branch and its tests and its paragraphs are all dead, why the check runs twice rather than once, since this project has two test suites standing in two different interpreters and either can be upgraded without the other, which is the hole that produced the first of these rules in the first place, why each failing path is asked for directly rather than only the healthy one, and why this is a development check and not a runtime one |
| [93-a-question-about-the-text-in-front.md](93-a-question-about-the-text-in-front.md) | Why the first construct RE2 has not got to land on the engine that copies Python is the lookahead rather than the backreference, even though the backreference is the bigger number, since a backreference asks what a group matched earlier in this attempt and the machine runs every live position in lockstep precisely so that it never has to hold such a thing, while a lookahead is a question about a position and so is the same shape of question the position instruction already answers, how the body is compiled into the same instruction list with a jump written over it so the outer walk never falls in from the front, why the second machine is seeded with exactly one attempt rather than one per position and what `(?=b)a` against `ab` would answer if it were not, why that machine reads the whole row rather than a slice starting at the position, since an anchor inside a lookahead is a question about the row and a slice would answer `a(?=b$)` against `abc` with a yes, why nesting works by recursion and what the buffers cost that has been left to be measured rather than guessed, why the lookbehind is a different question and not a harder version of this one, why a capturing group inside a lookahead is refused only when the caller asked for the groups, why an empty negative lookaround compiles to a set with no members and why both libraries still raise for it on `contains` and answer for it on `extract`, and why the DFA cache refuses the instruction outright, plus the three latent bugs the construct woke up, which are the routed branch of `program_for` that was unanchored and slotless and correct only for as long as everything it compiled was refused, the rewrite for `match` and `fullmatch` that renumbers every group and so turns a backreference into a name for its own wrapper, and Python's rule about what happens after a match of no width, which document 86 wrote down as a step one character on and which is really a second look at the same position with the end of the pattern refused there |
| [94-a-question-about-the-text-behind.md](94-a-question-about-the-text-behind.md) | Why the other half of the lookaround needed one function in the compiler and almost nothing else, since a lookahead starts its body where the thread is standing and a lookbehind has to start it far enough back that it ends there, which is a number only a body that always reads the same count of characters has, why refusing the bodies that have not got one is agreement with a rule of the language rather than a limitation this library is choosing, what the width walk says about each shape and why the members of a set are not walked into and why an assertion of any kind is worth nothing, which is what makes `(?<=^)` and `(?<=\b)` ordinary rather than special cases, why the width and the sign are packed into one payload rather than growing every instruction in every program by a third for the benefit of one of them, and why that is the opposite call from the bit that distinguishes the two forms of a lookahead without being inconsistent with it, why the second machine is the same second machine and does not know which direction it is answering, why nothing checks that the body ends where the thread is standing, why a body wider than the text behind is answered without running anything and what `(?<!ab)c` against `bc` says about it, why the nested machine can stop when no thread survives a pass and the outer one cannot, why the width refusal is a `ValueError` where every other refusal on this engine is a gap, and what the corpus did, which is 336 patterns held out for this construct becoming 290 answered, 26 refused in agreement and 20 held out for the next construct along |
| [95-a-question-about-the-path.md](95-a-question-about-the-path.md) | Why the backreference is a document about an engine rather than a document about a construct, since the state cache makes a state out of a set of instructions and the Pike machine merges two threads standing at the same instruction and the same position, and both of those rest on an instruction and a position saying everything about what is left to do, which a question about what the arriving path matched takes away, why that same sentence is what the backtracker's bitmap rests on and so why the bitmap goes off for a program holding one, what is left to bound the walk with when there is no second engine underneath to hand a hard row to and why a count of four million steps and a raise is a better answer than upstream's, which is a call that does not come back, why a pattern holding one carries slots the caller never asked for and why that is decided before the first instruction is written, why the ignore case flag is read where the instruction is emitted rather than where the tree is checked, what a backreference under that flag is actually compared by and why the fold tables this library carries are the wrong tool for it, why a group that never took part fails the reference where a group that matched nothing does not, why a lookaround beside one is refused and why that is checked over the emitted code rather than over the tree, and the capturing bracket the anchoring rewrite used to write, which numbered every group one higher and which nothing could reach until this slice |
| [96-three-steps-a-character-and-two-of-them-spare.md](96-three-steps-a-character-and-two-of-them-spare.md) | Why a class under a plus costs three instructions a character in a Thompson program and why two of the three are bookkeeping, why the character class loop RE2 answers this with is not available to a backtracker that has to keep one visit per instruction per position and what a pattern with two runs next to each other would cost if it were, what the smaller fusion is that keeps the bound exactly and why the order of the two pushes is the whole of its correctness, why the bitmap cells the body and the jump no longer get are not a hole, what happened when the shape was written into the program as two opcodes instead, which is that the Pike machine got about 1.4 times slower for a note it has no use for because its dispatch chain grew two comparisons in front of the leaf every character falls through, why a note read by three engines and wanted by one belongs beside the emit that makes it rather than inside the instruction set, and what the fusion is worth on the real column, which is about 1.8 times on the backtracker and about 1.6 on the kernel around it |
| [engine/](engine/00-README.md) | How DuckDB and Polars execute a query, and what firepanda is going to do about it |
| [planner/](planner/00-README.md) | What decides the shape of a query: the plan, the passes, predicate transfer, and choosing an operator |
| [sql/](sql/00-README.md) | DuckDB's dialect, the vendored grammar, the shared logical plan, and how compatibility gets measured |

## How to read this

**Three claims carry the rest of the design.** That a kernel monomorphized over
`DType` is competitive with hand-written intrinsics without the tail bugs, that the
Arrow C Data Interface can be implemented in Mojo with genuinely zero copies, and
that a self-contained wheel can be built despite an unstable ABI. Documents 03, 02
and 07 respectively. If one of those is false, the milestone that depends on it is
where the plan changes.

Two of those three claims have since been checked against a running toolchain rather than argued for, and both held: the Arrow C Data Interface landed at M2 and its zero copy is asserted by pointer address in the test suite, and a self contained wheel was built and imported from an interpreter with no Mojo toolchain reachable. Document 12 is that check, and it also records the two things that turned out not to work.

**Claims marked [verify] are not confirmed.** Mojo reached 1.0 recently and the
standard library moved substantially on the way there, so any sample older than
August 2026 is likely to be wrong about names. Where a claim came from a secondary
source it says so. Check them against the toolchain you install.

**There are no time estimates.** Milestones are ordered by dependency and by risk.
Sizing them in weeks invites a reader to add the numbers up and treat the total as a
delivery date, and at this stage that number would be fiction.

**Conventions.** Documents refer to each other as "document NN section N", and CI
checks that those references resolve. Milestone tags `(M0)` through `(M11)`, plus
`(M4b)`, in the parity checklist refer to headings in document 08, and CI checks that
too.
