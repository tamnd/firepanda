# pixi
.pixi/
*.pixi

# Mojo
*.mojopkg
*.o
.mojo_cache/

# Build output
build/
dist/
wheelhouse/
*.so
*.dylib
*.whl
*.tar.gz

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Benchmarks and profiling
current.json
baseline.json
bench.json
bench-*.json
compile-budget.json
*.prof
perf.data*

# Fuzzing corpus artifacts (the committed corpus lives under tests/corpus)
crash-*
timeout-*
leak-*

# Editors and OS
.DS_Store
.idea/
.vscode/
!.vscode/extensions.json

# Throwaway timing programs. They are written for one measurement and deleted.
tools/probes/

# A before and after measurement needs the old copy of a source file sitting in
# the tree, because the benchmark machines are handed a tarball rather than a
# clone and cannot check one out for themselves. They are scratch and one of
# them has already been committed by accident.
old*.txt
